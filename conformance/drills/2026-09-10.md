# Halt drills, 2026-09-10T20:17Z, gateway built from 3b84129

## 1. The evidence store cannot be written (row 7.6.3)
- a normal step first: HTTP 200, ALLOW | within grant
- store made unwritable, next step: HTTP 503, FAIL_CLOSED | evidence store unavailable: open /var/folders/ng/6yq5_hqn2xx6qsbzjj8127l00000gn/T/tmp.AnPD
- store restored, next step: HTTP 200, ALLOW | within grant
- failure log entries:        1
- verdict: the refused step was FAIL_CLOSED, nothing left, and the failure is itself on record. Holds.

## 2. The chain is tampered with (row 8.3.3)
- the first record's verdict rewritten on disk, gateway restarted
- next step: HTTP 403, None | the evidence log for did:example:drill#agent breaks at record 0; refusing to act on top of
- verdict: no step is judged on top of a rewritten chain. Holds.

## 3. Hardware attestation is required and cannot be produced (row 8.3.3, 7.2.3)
- the gateway refused to start: configured to attest on Intel TDX but cannot: no TDX quote provider on this machine: configfs-tsm: MacOS is unsupported;
- verdict: a gateway that must attest and cannot does not serve. Holds.

## Observed in production, not drilled
- 9 September 2026: the gateway on the confidential VM refused to start twice for lack of a usable quote provider (docs/tier-3-rehearsal.md).
- 10 September 2026 20:03 UTC: the far-end check on stocklistdealer's runner refused a dispatch (agent DID form) and the job stopped before any package moved (conformance/statement.md).
- 10 September 2026: the daily verifier reported the anchored attestation unresolvable after two reboots; the anchor job halted on it until every quote was kept (control v0.8.1).
